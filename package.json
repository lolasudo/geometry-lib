{
  "name": "geometry-lib",
  "version": "1.0.0",
  "description": "TypeScript библиотека для работы с геометрическими фигурами",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "clean": "del /f /s /q dist 2>nul || rmdir /s /q dist",
    "prebuild": "npm run clean",
    "dev": "tsc --watch",
    "example": "tsc && node dist/example.js"
  },
  "keywords": ["geometry", "shapes", "typescript"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/uuid": "^9.0.7",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "uuid": "^9.0.1"
  }
}